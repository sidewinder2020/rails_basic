<h1> Spoiled Brats </h1>
<ul>
  <% @students.each do |student| %>
    <li><%= link_to student.first_name, student_path(student.id) %></li>
  <% end %>
</ul>

<%= link_to "Course List", courses_path %>
